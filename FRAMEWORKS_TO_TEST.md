# Frameworks Available for Testing

## Already Tested ✅
1. ✅ OpenAI SDK (Python)
2. ✅ Anthropic SDK (Python)
3. ✅ LangChain (Python)

## Tier 1 Frameworks (High Priority)

### Python SDKs (Easy to Test)
4. **Google Generative AI SDK (Gemini)** - `pip install google-generativeai`
5. **Cohere SDK** - `pip install cohere`
6. **Mistral AI SDK** - `pip install mistralai`
7. **Together AI SDK** - `pip install together`
8. **Perplexity API** - `pip install perplexity-ai`
9. **AI21 Labs SDK** - `pip install ai21`
10. **Replicate SDK** - `pip install replicate`
11. **Stability AI SDK** - `pip install stability-sdk`

### Python Frameworks
12. **LlamaIndex** - `pip install llama-index`
13. **CrewAI** - `pip install crewai`
14. **AutoGen (Microsoft)** - `pip install pyautogen`
15. **Haystack** - `pip install haystack-ai`
16. **Hugging Face transformers** - `pip install transformers`

### Local/Server Frameworks
17. **Ollama Python client** - `pip install ollama`
18. **GPT4All** - `pip install gpt4all`

## Tier 2 Frameworks (Medium Priority)

19. **AutoGPT** - More complex, requires setup
20. **Semantic Kernel** - Microsoft framework
21. **LangFlow** - UI-based, harder to automate
22. **GPT-Engineer** - Project generator
23. **LocalAI** - Local server setup
24. **Cheshire Cat AI** - Local server

## Tier 3 Frameworks (Lower Priority)

25. **LM Studio API client** - Local server
26. **Jan AI client** - Local server
27. **RunPod client** - Cloud platform

## JavaScript/Node.js Frameworks

28. **LangChain.js** - `npm install langchain`
29. **Ollama JavaScript client** - `npm install ollama`

## Go Frameworks

30. **Ollama server** - Go application

---

## Recommended Testing Order

### Easy to Test (Start Here):
1. Google Generative AI SDK
2. Cohere SDK
3. Mistral AI SDK
4. Together AI SDK
5. LlamaIndex
6. CrewAI
7. Ollama Python client

### Medium Difficulty:
8. AutoGen
9. Haystack
10. Hugging Face transformers

### Requires Setup:
- AutoGPT
- Semantic Kernel
- Local servers (Ollama, LocalAI, etc.)

---

## Installation Commands

```bash
# Python SDKs
pip install google-generativeai
pip install cohere
pip install mistralai
pip install together
pip install perplexity-ai
pip install ai21
pip install replicate
pip install stability-sdk

# Python Frameworks
pip install llama-index
pip install crewai
pip install pyautogen
pip install haystack-ai
pip install transformers
pip install ollama
pip install gpt4all

# JavaScript (if testing Node.js)
npm install langchain
npm install ollama
```

